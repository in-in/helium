import React from "react"
import Container from "../container"
import st from "./styles.module.scss"

const Customers = () => (
  <section className={st.customers}>
    <Container>
      <p>Customers component</p>
    </Container>
  </section>
)

export default Customers
